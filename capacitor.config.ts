import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.synthetiq.invoices',
  appName: 'Synthetiq Invoices',
  webDir: 'dist',
};

export default config;

